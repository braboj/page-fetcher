# Normalize line endings to LF in the repository, whatever the checkout OS.
* text=auto eol=lf

# Captured HTML fixtures are frozen byte-for-byte snapshots of real
# responses. Line-ending normalization would change the test input, so
# treat them as binary.
pagefetch/tests/fixtures/** -text
